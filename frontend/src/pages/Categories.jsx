import { Tags } from "lucide-react";
import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function Categories() {
  return (
    <PagePlaceholder
      icon={Tags}
      title="Categories"
      description="Organize your products into categories for easier browsing and reporting."
    />
  );
}
